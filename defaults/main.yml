aipscan_install_dir: "/usr/share/archivematica/AIPscan"
aipscan_virtualenv: "/usr/share/archivematica/virtualenvs/AIPscan"
aipscan_branch: "main"
aipscan_user: "archivematica"
aipscan_group: "archivematica"
aipscan_listen_port: "8057"
systemd_environment_path: "{{ '/etc/default' if ansible_os_family == 'Debian' else '/etc/sysconfig' }}"
#aipscan_typesense_api_key: ""

# Define the following in host_vars when using a mysql database
# aipscan_db_user
# aipscan_db_pass
# aipscan_db_host
# aipscan_db_name
